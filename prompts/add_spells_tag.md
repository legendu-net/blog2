Add a sub-command `add_spells_tag` with the alias `astag` into `blog.py`.
Please refer to the implementation of the sub-command `add_spells_title` 
as they should be largely the same.
You have my permissions to make necessary changes in `blog.py` and `blogger.py`,
however,
do not commit changes so that I can review it.
