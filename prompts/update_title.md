Help me implement the method `update_title` in blogger.py.
Please refer to the implementation of `update_tags`
as they should be largely the same.
However, 
leverage the function `format_title` to get the desired title.
Please also add a sub-command `update_title` with the alias `utitle` into blog.py just like what we have for `update_tags`.
